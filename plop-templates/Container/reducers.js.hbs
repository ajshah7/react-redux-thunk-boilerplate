import { DEFAULT_ACTION } from './constants'; import { API_CONSTANTS } from
'../../constants'; const initialState = { } export default (state =
initialState, { type, payload }) => { switch (type) { case DEFAULT_ACTION:
return state; default: return state } }