import { DEFAULT_ACTION } from './constants'; import { API_CONSTANTS } from
'../../constants'; const initialState = { }; const
{{pascalCase name}}
= (state = initialState, { type, payload }) => { switch (type) { case
DEFAULT_ACTION: return state; default: return state } }; export default
{{pascalCase name}}